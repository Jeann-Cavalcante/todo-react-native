import { Button, Container, Input, Text } from "./styles";

const Inputodo = () => {
  return (
    <Container>
      <Input />
      <Button>
        <Text>
        +
        </Text>
      </Button>
    </Container>
  );
};

export default Inputodo;
